# Hobby video files

Drop `.mp4` (or other browser-supported) video files here. They will be served at `/videos/filename.mp4`.

To show a video in the hobby carousel lightbox, set `videoKey` on that hobby in `src/data/portfolio.ts` to the filename (e.g. `"demo.mp4"`).
